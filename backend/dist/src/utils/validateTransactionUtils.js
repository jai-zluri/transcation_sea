"use strict";
// export const isValidTransaction = (date: string, amount: number): boolean => {
//   // Check if the date and amount are valid
//   if (!date || !amount) {
//     return false; // Return false if either date or amount is invalid
//   }
//   // Additional checks for transaction validity (you can expand these as needed)
//   const isDateValid = !isNaN(new Date(date).getTime());
//   const isAmountValid = !isNaN(amount) && amount > 0;
//   // Return the result as a boolean (ensuring it is always a boolean)
//   return isDateValid && isAmountValid;
// };
